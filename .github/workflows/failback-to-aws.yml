# =============================================================================
# Workflow: Failback a AWS
# =============================================================================
# Restaura la operación en AWS después de un failover a Azure.
#
# Pasos (se completarán en el segundo despliegue):
#   1. mysqldump de Azure DB
#   2. Restaurar dump en RDS de AWS
#   3. Verificar datos en AWS
#   4. terraform destroy en Azure
#   5. Actualizar estado: active_cloud = aws
#
# PENDIENTE: Este workflow se desarrollará junto con el failover.
# =============================================================================

name: Failback to AWS

on:
  workflow_dispatch:

jobs:
  failback:
    name: Failback a AWS
    runs-on: ubuntu-latest

    steps:
      - name: Checkout del repositorio
        uses: actions/checkout@v4

      - name: Placeholder - Failback a AWS
        run: |
          echo "==========================================="
          echo "FAILBACK A AWS - PENDIENTE DE IMPLEMENTAR"
          echo "==========================================="
          echo ""
          echo "Este workflow se completará en la fase de DR:"
          echo "  1. mysqldump de Azure DB"
          echo "  2. Restaurar en RDS de AWS"
          echo "  3. Verificar health check en AWS"
          echo "  4. terraform destroy terraform/azure/"
          echo "  5. Actualizar active_cloud = aws"
